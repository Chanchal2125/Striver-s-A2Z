void insertionSort(int arr[], int n)
{
    //write your code here
    for(int i = 0; i < n; i++){
        int m = i;
        while(m > 0 && arr[m-1] > arr[m]){
            int t = arr[m-1];
            arr[m-1] = arr[m];
            arr[m] = t;
            m--;
        }
    }
}
